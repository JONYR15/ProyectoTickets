@model IEnumerable<Backend.Entities.Status>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row">
    <div class="col-12 text-center">
        <h2>Estatus</h2>
        <hr class="hrn-hr" />
    </div>
</div>

<p>
    <div class="col-12 text-justify">
        <a asp-action="Create" class="btn btn-secondary" style="color:white;">Crear Estatus</a>
</p>
@*<table class="table">
    <thead *@
<div class="row">
    <div class="col-2"></div>
    <div class="table-responsive col-8">
        <table id="tblStatus" class="table table-striped table-bordered table-hover">
            <thead class="bg-primary">
                <tr>
                    <th>
                        <font color="white">ID</font>
                    </th>
                    <th class="">
                        <font color="white">Tipo de Estatus</font>
                    </th>
                    <th style="width: 250px;">
                        <font color="white">Acciones</font>
                    </th>

                    @*<th>
                            @Html.DisplayNameFor(model => model.Id)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Description)
                        </th>*@
       
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>

                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                       
                        @*<td>

                                @Html.ActionLink("Editar", "Edit", new { id = item.Id }) |
                                @Html.ActionLink("Detalles", "Details", new { id = item.Id }) |
                                @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
                            </td>*@

                        <td class="col-4" style="display: inline-flex; width: 250px; " >
                            <a href="../Status/Edit/@item.Id" class="btn btn-facebook" style="color:white;">Editar</a>
                            <a href="../Status/Details/@item.Id" class="btn btn-facebook" style="color:white;">Detalles</a>
                            <a href="../Status/Delete/@item.Id" class="btn btn-danger" style="color:white;">Borrar</a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>




